Silverton Services
=================

This site uses static HTML, CSS, and JavaScript.

To preview locally, open index.html in a browser.

For the protected hero-image upload flow, this environment does not currently have a Node or Python runtime available, so the upload form is wired to a placeholder endpoint. In a hosted environment with a runtime, the included server.js can be used.
